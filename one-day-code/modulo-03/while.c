#include <stdio.h>
#include <stdlib.h>

int main()
{
    int j = 0, x = 10;

    while (j < 10) {
        printf("\n%d", j);
        j++;
    }

    while (x >= 0 ) {
        printf("\n%d", x);
        x--;
    }

    printf("\n");

    return 0;
}
